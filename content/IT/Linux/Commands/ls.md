---
tags:
  - it/linux
---



**full timestamps with year, month, day, and time** regardless of age
```bash
ls --full-time
```

`-t` sort by modification time, newest first (descending)
`-r` reverses the order. Oldest first (ascending):
```bash
ls -ltr
```










